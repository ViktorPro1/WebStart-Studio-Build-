# 🤝 Внесок у розробку WebStart Studio

Дякуємо за інтерес до нашого проєкту! Ми раді будь-якому внеску — від виправлення помилок до нових функцій.

## 📋 Зміст

- [Як почати](#як-почати)
- [Процес розробки](#процес-розробки)
- [Код-стайл](#код-стайл)
- [Тестування](#тестування)
- [Pull Request процес](#pull-request-процес)

## 🚀 Як почати

### 1. Форкніть репозиторій

Натисніть кнопку "Fork" у правому верхньому куті GitHub.

### 2. Клонуйте свій форк

```bash
git clone https://github.com/ваш-username/WebStart-Studio-Build-.git
cd WebStart-Studio-Build-
```

### 3. Додайте upstream remote

```bash
git remote add upstream https://github.com/ViktorPro1/WebStart-Studio-Build-.git
```

### 4. Створіть нову гілку

```bash
git checkout -b feature/ваша-нова-фіча
```

## 🔧 Процес розробки

### Структура проєкту

```
WebStart-Studio-Build-/
├── css/              # Стилі
├── scripts/          # JavaScript файли
├── pages/            # HTML сторінки
├── images/           # Зображення
├── documents/        # Документи (брифи, гайди)
└── international/    # Мультимовна підтримка
```

### Встановлення залежностей

Проєкт не потребує npm/yarn — це чистий HTML/CSS/JS.

### Запуск локально

Просто відкрийте `index.html` у браузері або використайте Live Server:

- VS Code: встановіть розширення "Live Server"
- Натисніть "Go Live" у статус-барі

## 🎨 Код-стайл

### HTML

- Використовуйте семантичні теги (`<section>`, `<article>`, `<nav>`)
- Додавайте `alt` для всіх зображень
- Дотримуйтесь вкладеності (indent: 4 пробіли)

```html
<section id="services">
  <h2>Наші послуги</h2>
  <div class="services-grid">
    <article class="service-card">
      <h3>Дизайн портфоліо</h3>
    </article>
  </div>
</section>
```

### CSS

- Використовуйте класи, уникайте ID для стилів
- Дотримуйтесь BEM-нотації де можливо
- Групуйте властивості логічно

```css
.service-card {
  /* Позиціювання */
  position: relative;

  /* Блокова модель */
  display: flex;
  padding: 20px;

  /* Візуальні властивості */
  background: #fff;
  border-radius: 8px;

  /* Типографіка */
  font-size: 16px;
}
```

### JavaScript

- Використовуйте `const` за замовчуванням, `let` — за потреби
- Іменуйте функції описово
- Додавайте коментарі для складної логіки

```javascript
// Ініціалізація темної теми
function initDarkMode() {
  const savedTheme = localStorage.getItem("theme");
  if (savedTheme === "dark") {
    document.body.classList.add("dark-mode");
  }
}
```

### Іменування

- Файли: kebab-case (`project-checker.html`)
- Класи CSS: kebab-case (`.service-card`)
- JavaScript змінні: camelCase (`darkModeToggle`)
- Константи: UPPER_SNAKE_CASE (`MAX_FILE_SIZE`)

## 🧪 Тестування

### Мануальне тестування

Перевірте вашу функціональність на:

- Chrome (останні 2 версії)
- Firefox (останні 2 версії)
- Safari (якщо доступний)
- Мобільних пристроях (responsive design)

### Чеклист перед PR

- [ ] Код працює без помилок у консолі
- [ ] Адаптивний дизайн працює (320px - 1920px)
- [ ] Всі зображення мають `alt` атрибути
- [ ] Немає console.log() у production коді
- [ ] Файли оптимізовані (мінімізовані якщо потрібно)

## 📝 Pull Request процес

### 1. Оновіть свою гілку

```bash
git fetch upstream
git rebase upstream/main
```

### 2. Закомітьте зміни

```bash
git add .
git commit -m "feat: додано новий генератор промптів"
```

### Формат commit повідомлень

```
type(scope): короткий опис

[опціонально: детальний опис]

[опціонально: BREAKING CHANGE: опис змін]
```

**Types:**

- `feat`: нова функція
- `fix`: виправлення бага
- `docs`: зміни в документації
- `style`: форматування, пропущені крапки з комою тощо
- `refactor`: рефакторинг коду
- `perf`: покращення продуктивності
- `test`: додавання тестів
- `chore`: зміни в build process

**Приклади:**

```
feat(ai-service): додано AI автоматизацію для створення контенту
fix(navbar): виправлено баг з мобільним меню
docs(readme): оновлено інструкції для встановлення
```

### 3. Створіть Pull Request

1. Запушіть зміни: `git push origin feature/ваша-фіча`
2. Відкрийте PR на GitHub
3. Опишіть зміни детально
4. Прикріпіть скріншоти (якщо UI зміни)
5. Згадайте пов'язані issues: `Fixes #123`

### Шаблон опису PR

```markdown
## Що змінено

Короткий опис змін

## Тип зміни

- [ ] Виправлення бага
- [ ] Нова функція
- [ ] Зміна в документації
- [ ] Рефакторинг

## Як тестували

Опишіть процес тестування

## Скріншоти (якщо застосовно)

Додайте зображення

## Чеклист

- [ ] Код відповідає стайлгайду
- [ ] Додано коментарі для складної логіки
- [ ] Документація оновлена
- [ ] Протестовано на різних пристроях
```

## 🐛 Репорт про баги

Використовуйте GitHub Issues з таким шаблоном:

```markdown
**Опис бага**
Чіткий опис проблеми

**Як відтворити**

1. Перейдіть до '...'
2. Натисніть на '...'
3. Прокрутіть до '...'
4. Побачите помилку

**Очікувана поведінка**
Що повинно статися

**Скріншоти**
Додайте скріншоти

**Середовище:**

- ОС: [напр. Windows 10]
- Браузер: [напр. Chrome 120]
- Розширення екрану: [напр. 1920x1080]
```

## 💡 Пропозиції нових функцій

Створіть Issue з міткою "enhancement":

```markdown
**Ваша ідея**
Опишіть функціонал

**Проблема яку вирішує**
Яку проблему це вирішить?

**Альтернативи**
Чи розглядали ви інші варіанти?

**Додаткова інформація**
Скріншоти, посилання, приклади
```

## 🌍 Переклади

Додавання нової мови:

1. Створіть файл у `international/вашамова.html`
2. Скопіюйте структуру з `international/en.html`
3. Перекладіть весь контент
4. Додайте прапор у `international/flags/`
5. Оновіть `index.html` у секції `#international`

## ❓ Потрібна допомога?

- 💬 Telegram: https://t.me/+IleSiwteF2NlOWVi
- 📱 Viber: +38(066) 139 19 32
- 📧 Email: webstartstudio978@gmail.com

## 📜 Ліцензія

Додаючи код до цього репозиторію, ви погоджуєтеся, що ваш внесок буде ліцензовано під тією ж ліцензією, що й проєкт.

---

**Дякуємо за ваш внесок у WebStart Studio! 🎉**
