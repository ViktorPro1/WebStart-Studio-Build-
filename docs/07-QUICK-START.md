# ⚡ Швидкий Старт WebStart Studio

## 📋 Вимоги

### Мінімальні Вимоги

- **Браузер:** Будь-який сучасний (Chrome, Firefox, Safari, Edge)
- **Текстовий редактор:** VS Code, Sublime Text, або будь-який інший
- **Git:** Для клонування репозиторію (опціонально)

### Для Розробки PWA

- **Node.js:** v14+ (для локального сервера)
- **NPM:** v6+ (для build скриптів)

---

## 🚀 Встановлення

### Варіант 1: Прямий Перегляд

Найпростіший спосіб - просто відкрити файл у браузері:

```bash
# 1. Завантажте проєкт
git clone https://github.com/ViktorPro1/WebStart-Studio-Build-.git

# 2. Відкрийте index.html у браузері
# Просто двічі клацніть на файл
```

**Плюси:**

- ✅ Миттєвий старт
- ✅ Не потрібні додаткові інструменти

**Мінуси:**

- ❌ Service Worker не працює
- ❌ Деякі фічі можуть не функціонувати

### Варіант 2: Локальний Сервер (Рекомендовано)

Для повноцінної роботи PWA потрібен локальний веб-сервер:

#### Використовуючи http-server (Node.js)

```bash
# 1. Встановіть http-server глобально
npm install -g http-server

# 2. Перейдіть до директорії проєкту
cd WebStart-Studio-Build-

# 3. Запустіть сервер
http-server

# 4. Відкрийте в браузері
# http://localhost:8080
```

#### Використовуючи Python

```bash
# Python 3
python -m http.server 8000

# Python 2
python -m SimpleHTTPServer 8000

# Відкрийте http://localhost:8000
```

#### Використовуючи VS Code Live Server

```bash
# 1. Встановіть розширення "Live Server"
# 2. Відкрийте index.html
# 3. Натисніть "Go Live" внизу справа
```

---

## 🛠️ Налаштування Середовища

### VS Code Extensions

Рекомендовані розширення для розробки:

```json
{
  "recommendations": [
    "ritwickdey.liveserver", // Live Server
    "esbenp.prettier-vscode", // Prettier
    "dbaeumer.vscode-eslint", // ESLint
    "bradlc.vscode-tailwindcss", // Tailwind IntelliSense
    "zignd.html-css-class-completion", // CSS Class completion
    "formulahendry.auto-rename-tag", // Auto Rename Tag
    "ms-vscode.vscode-typescript-next" // TypeScript
  ]
}
```

### EditorConfig

Файл `.editorconfig` вже налаштований:

```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
```

---

## 📂 Структура Проєкту

### Основні Файли

```
WebStart-Studio-Build-/
├── index.html              # Головна сторінка - СТАРТОВА ТОЧКА
├── djon.html               # Навігаційний гід
├── cabinet.html            # Кабінет клієнта
│
├── css/                    # Модульні стилі
│   └── main.css           # Імпорт всіх стилів
│
├── scripts/                # JavaScript модулі
│   └── index.js           # Головний скрипт
│
├── sw.js                   # Service Worker (PWA)
├── manifest.json           # PWA маніфест
│
└── images/                 # Зображення (WebP)
```

### Де Що Редагувати

| Що потрібно змінити | Файл                      |
| ------------------- | ------------------------- |
| Контент сторінки    | `index.html`              |
| Стилі секцій        | `css/sections/*.css`      |
| JavaScript логіка   | `scripts/modules/**/*.js` |
| PWA налаштування    | `manifest.json`           |
| Офлайн-кеш          | `sw.js`                   |

---

## 🎨 Перші Кроки Розробки

### 1. Зміна Кольорів

**Файл:** `css/base/reset.css`

```css
:root {
  /* Змініть ці змінні */
  --primary: #2563eb; /* Основний колір */
  --secondary: #10b981; /* Додатковий колір */
  --accent: #f59e0b; /* Акцент */
}
```

### 2. Додавання Секції

**1) Створіть новий CSS файл:**

```bash
# css/sections/my-section.css
```

**2) Додайте стилі:**

```css
.my-section {
  padding: var(--space-16) 0;
  background: var(--bg-primary);
}
```

**3) Імпортуйте в main.css:**

```css
@import "sections/my-section.css";
```

**4) Додайте HTML в index.html:**

```html
<section class="my-section">
  <h2>Моя Нова Секція</h2>
  <p>Контент секції...</p>
</section>
```

### 3. Додавання JavaScript Функції

**1) Створіть модуль:**

```bash
# scripts/modules/features/myFeature.js
```

**2) Експортуйте функцію:**

```javascript
export function initMyFeature() {
  console.log("Моя фіча ініціалізована");

  // Ваш код тут
}
```

**3) Імпортуйте в index.js:**

```javascript
import { initMyFeature } from "./modules/features/myFeature.js";

// Викличте при завантаженні
document.addEventListener("DOMContentLoaded", () => {
  initMyFeature();
});
```

---

## 🔧 Build Процес

### CSS Компіляція

**1) Встановіть залежності:**

```bash
npm install
```

**2) Запустіть білд:**

```bash
npm run build:css
```

**3) Результат:**

```
dist/styles.css створено ✅
```

### Автоматичний Watch

```bash
# Автоматична перекомпіляція при змінах
npm run watch:css
```

---

## 🧪 Тестування

### Перевірка PWA

**1) Відкрийте Chrome DevTools:**

```
F12 → Application → Service Workers
```

**2) Перевірте статус:**

- ✅ Service Worker активний
- ✅ Кеш створений
- ✅ Офлайн-режим працює

**3) Тест офлайн:**

```
DevTools → Network → Offline
Оновіть сторінку - має працювати!
```

### Lighthouse Аудит

**1) Відкрийте DevTools:**

```
F12 → Lighthouse
```

**2) Налаштуйте:**

- ✅ Performance
- ✅ Accessibility
- ✅ Best Practices
- ✅ SEO
- ✅ PWA

**3) Generate report**

**Цільові показники:**

- Performance: 90+
- Accessibility: 95+
- Best Practices: 100
- SEO: 100

---

## 📱 Тестування на Мобільних

### Chrome DevTools

```
F12 → Toggle Device Toolbar (Ctrl+Shift+M)
```

**Протестуйте на:**

- iPhone 12/13/14
- iPad
- Samsung Galaxy S20+
- Pixel 5

### Реальні Пристрої

**1) Знайдіть локальну IP:**

```bash
# Windows
ipconfig

# Mac/Linux
ifconfig
```

**2) Відкрийте на телефоні:**

```
http://YOUR_IP:8080
```

---

## 🚀 Деплой

### Netlify (Рекомендовано)

**1) Створіть файл `netlify.toml`:**

```toml
[build]
  publish = "."
  command = "npm run build:css"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "14"
```

**2) Підключіть GitHub репозиторій:**

- Зайдіть на netlify.com
- New site from Git
- Виберіть репозиторій
- Deploy site

**3) Готово!**

```
https://your-site.netlify.app
```

### GitHub Pages

**1) В `package.json` додайте:**

```json
{
  "homepage": "https://username.github.io/repo-name",
  "scripts": {
    "predeploy": "npm run build:css",
    "deploy": "gh-pages -d ."
  }
}
```

**2) Встановіть gh-pages:**

```bash
npm install --save-dev gh-pages
```

**3) Деплой:**

```bash
npm run deploy
```

### Vercel

```bash
# Встановіть Vercel CLI
npm i -g vercel

# Деплой
vercel
```

---

## 🐛 Troubleshooting

### Service Worker не працює

**Проблема:** SW не реєструється

**Рішення:**

1. Перевірте, чи використовуєте HTTPS або localhost
2. Очистіть кеш браузера
3. Видаліть старий SW: `DevTools → Application → Service Workers → Unregister`

### Стилі не застосовуються

**Проблема:** Зміни в CSS не відображаються

**Рішення:**

1. Запустіть `npm run build:css`
2. Очистіть кеш браузера (Ctrl+Shift+R)
3. Перевірте шлях до CSS файлу

### JavaScript помилки

**Проблема:** Модулі не імпортуються

**Рішення:**

1. Перевірте, чи використовуєте `type="module"` в script tag
2. Перевірте шляхи імпортів
3. Відкрийте Console для деталей

---

## 📚 Корисні Команди

```bash
# Клонування проєкту
git clone https://github.com/ViktorPro1/WebStart-Studio-Build-.git

# Встановлення залежностей
npm install

# Запуск локального сервера
npm run dev

# Білд CSS
npm run build:css

# Автоматичний білд при змінах
npm run watch:css

# Деплой
npm run deploy

# Перевірка лінтингу
npm run lint

# Форматування коду
npm run format
```

---

## 🎓 Навчальні Ресурси

### Офіційна Документація

- [MDN Web Docs](https://developer.mozilla.org/)
- [Web.dev](https://web.dev/)
- [PWA Builder](https://www.pwabuilder.com/)

### Проєкт-Специфічні

- [Документація проєкту](README.md)
- [Технічна архітектура](02-TECHNICAL-ARCHITECTURE.md)
- [Ключові функції](03-KEY-FEATURES.md)

---

## 💡 Поради для Початківців

1. **Почніть з малого**

   - Спочатку змініть текст
   - Потім кольори
   - Потім додайте нову секцію

2. **Використовуйте DevTools**

   - Експериментуйте зі стилями
   - Дивіться помилки в Console
   - Тестуйте на різних пристроях

3. **Вчіться на прикладах**

   - Вивчайте існуючий код
   - Копіюйте патерни
   - Адаптуйте під свої потреби

4. **Не бійтеся ламати**
   - Git дозволяє відкотити зміни
   - Експериментуйте
   - Вчіться на помилках

---

## 📞 Потрібна Допомога?

- 🐛 [Повідомити про баг](https://github.com/ViktorPro1/WebStart-Studio-Build-/issues/new?template=bug_report.md)
- 💡 [Запропонувати фічу](https://github.com/ViktorPro1/WebStart-Studio-Build-/issues/new?template=feature_request.md)
- 💬 [Обговорення](https://github.com/ViktorPro1/WebStart-Studio-Build-/discussions)
- 📧 [Email](mailto:webstartstudio978@gmail.com)

---

## 📚 Подальше Читання

- [Історія Оновлень →](08-UPDATE-HISTORY.md)
- [Як Долучитися →](../CONTRIBUTING.md)
- [Технічна Архітектура →](02-TECHNICAL-ARCHITECTURE.md)

---

[⬆ До змісту документації](README.md) | [← Попередній розділ](06-PERFORMANCE-METRICS.md) | [Наступний розділ →](08-UPDATE-HISTORY.md)
